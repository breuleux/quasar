
require:
   .util ->
      ID, methods
   .proxy ->
      deprox

provide:
   iterate-properties
   ;; iterate-properties2


iterate-properties{match obj} =
   null? or undefined? ->
      {}
   else ->
      deprox{obj}[methods.iterate-properties]{}


;; iterate-properties2{match obj} =
;;    null? or undefined? ->
;;       {}
;;    else ->
;;       deprox{obj}[methods.iterate-properties]{}

